{
  "name": "@ghosted/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Ghosted Hub — Shared types, Zod schemas, and constants",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@ghosted/config": "workspace:*",
    "tsup": "^8.4.0",
    "typescript": "^5.8.0"
  }
}
